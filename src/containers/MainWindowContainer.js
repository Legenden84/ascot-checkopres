import { connect } from 'react-redux';
import MainWindow from '../components/MainWindow';

// No mapStateToProps or mapDispatchToProps needed for MainWindow yet
export default connect(null, null)(MainWindow);
